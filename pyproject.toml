[project]
name = "wife-bain1"
version = "0.1.0"
description = "Binance Spot trading bot with a Vercel serverless handler"
requires-python = ">=3.12,<3.13"
dependencies = [
  "ccxt",
  "python-dotenv",
]

[tool.vercel]
entrypoint = "api.trade:handler"
